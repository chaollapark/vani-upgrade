{
  "name": "vani-ui-regression",
  "private": true,
  "version": "1.0.0",
  "description": "UI regression checks for vani wikis",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0"
  }
}
